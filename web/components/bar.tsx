import Burger from "@/components/nav/burger";

const Bar = () => {
  return (
    <section className={"fixed w-full flex justify-center items-center"}>
      <Burger />
    </section>
  );
};

export default Bar;
