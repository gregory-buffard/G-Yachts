import Bar from "@/components/bar";

const Home = () => {
  return (
    <main className="">
      <Bar />
      <section className={"w-screen h-screen bg-hero-banner"}></section>
    </main>
  );
};

export default Home;
